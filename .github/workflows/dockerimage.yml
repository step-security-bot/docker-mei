# This is a basic workflow to help you get started with Actions

name: build-mei-guidelines-image

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Build docker image
        run: docker build -t docker-mei-guidelines-container-image .
      
      - run: echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u riedde --password-stdin
    #- docker run --privileged --rm tonistiigi/binfmt --install all
    #- docker buildx create --name mybuilder --use
    #- docker buildx build --build-arg CI_JOB_TOKEN=${CI_JOB_TOKEN} --platform linux/arm64,linux/amd64 --push -t ${IMAGE_TAG_BASE} -f Dockerfile.baseImage .
      - run: docker push ghcr.io/riedde/docker-mei-guidelines-container-image:latest

      # Add multiplatform build